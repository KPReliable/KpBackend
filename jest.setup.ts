import "dotenv/config";

// Optional global setup for Jest tests:
// - add custom matchers
// - set up global variables
// - initialize test DB connections

// Example:
// beforeAll(async () => {
//   await someTestSetup();
// });

// afterAll(async () => {
//   await cleanupResources();
// });
